$("#banner").slick({
    // 按鈕播放速度
    speed: 500,
    // 自動播放 布林Boolean
    autoplay: true,
    // 自動播放速度
    autoplaySpeed: 3500,
    // 下方點點
    dots: true
});

$("#service").slick({slidesToShow:3});